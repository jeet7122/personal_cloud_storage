spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  application:
    name: photo-store-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/personal_storage
    username: postgres
    password: Khushi@26
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

cloudflare:
  r2:
    access-key: ${R2_ACCESS_KEY}
    secret-key: ${R2_SECRET_KEY}
    endpoint: https://89533f8f9d7afdc40e6210cac778a425.r2.cloudflarestorage.com
    bucket: personal-cloud